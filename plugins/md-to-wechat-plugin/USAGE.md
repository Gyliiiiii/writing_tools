# Markdown 转微信公众号 Plugin - 使用指南

## 📦 Plugin 已创建完成

位置：`/Users/feifei/Documents/AI/writing_ai_tools/md-to-wechat-plugin`

## ✅ 包含的功能

### 1. Markdown 转 HTML
- 脚本：`md-to-html.js`
- 功能：将 Markdown 转换为带内联样式的 HTML
- 样式：完美适配微信公众号

### 2. HTML 发布到微信
- 脚本：`html-to-wechat-draft.js`
- 功能：上传 HTML 到微信公众号草稿箱
- 支持：封面图片上传、自动获取 Access Token

## 📋 安装步骤

### 步骤 1: 安装依赖

```bash
cd /Users/feifei/Documents/AI/writing_ai_tools/md-to-wechat-plugin/skills/md-to-wechat
npm install
```

### 步骤 2: 复制到 Claude Code plugins 目录

```bash
cp -r /Users/feifei/Documents/AI/writing_ai_tools/md-to-wechat-plugin ~/.claude/plugins/
```

### 步骤 3: 重启 Claude Code

重启后，plugin 会自动加载。

## 🎯 使用方法

### 首次使用

在 Claude Code 中说：

```
把 article.md 发布到微信公众号
封面图：cover.jpg
```

Claude 会：
1. 检测到没有配置
2. 获取你的公网 IP
3. 提示你添加 IP 白名单
4. 询问 AppID、AppSecret、作者名称
5. 保存配置到 .env 文件

### 后续使用

配置完成后，直接说：

```
把 my-article.md 发布到公众号
封面：cover.png
```

Claude 会自动：
1. 读取 Markdown 文件
2. 转换为 HTML
3. 上传封面图片
4. 创建草稿
5. 返回 Media ID

## 🔧 手动配置（可选）

如果你想手动配置，可以直接创建 `.env` 文件：

```bash
cd ~/.claude/plugins/md-to-wechat/skills/md-to-wechat
cat > .env << 'ENVEOF'
WECHAT_APP_ID=你的AppID
WECHAT_APP_SECRET=你的AppSecret
DEFAULT_AUTHOR=你的名字
ENVEOF
```

## 📝 配置微信公众平台

### 1. 获取凭证

1. 登录 https://mp.weixin.qq.com
2. 进入「开发」→「基本配置」
3. 复制 AppID 和 AppSecret

### 2. 配置 IP 白名单

1. 在「基本配置」页面找到「IP白名单」
2. 点击「修改」
3. 添加你的公网 IP（Claude 会告诉你）
4. 保存并等待 5 分钟生效


## ✅ 测试

Plugin 已经过测试，包含：

1. ✅ Markdown 转 HTML 功能正常
2. ✅ 内联样式正确应用
3. ✅ 支持所有常见 Markdown 语法
4. ✅ 生成的 HTML 适配微信公众号

测试文件：
- 输入：`test-article.md`
- 输出：`test-output.html`

## 🐛 故障排除

### 问题 1: 获取 Access Token 失败

**原因**：AppID 或 AppSecret 错误

**解决**：
1. 检查 `.env` 文件中的凭证
2. 重新从微信公众平台复制

### 问题 2: IP 白名单错误

**原因**：IP 未添加到白名单

**解决**：
1. 运行 `curl -s https://api.ipify.org` 获取 IP
2. 在微信公众平台添加该 IP
3. 等待 5 分钟生效


### 问题 3: 封面图片上传失败

**原因**：图片大小超过 64KB 或格式不对

**解决**：
1. 压缩图片到 64KB 以下
2. 转换为 JPG 或 PNG 格式
3. 建议尺寸：900x500 像素

### 问题 4: 草稿创建失败

**原因**：草稿箱已满（最多 100 篇）

**解决**：
1. 登录微信公众平台
2. 删除一些旧草稿
3. 重新尝试

## 📂 文件结构

```
md-to-wechat-plugin/
├── plugin.json                    # Plugin 配置
├── README.md                      # 项目说明
├── USAGE.md                       # 使用指南（本文件）
├── test-article.md                # 测试文件
├── test-output.html               # 测试输出
└── skills/
    └── md-to-wechat/
        ├── SKILL.md               # Skill 文档
        ├── md-to-html.js          # Markdown 转 HTML
        ├── html-to-wechat-draft.js # 发布到微信
        ├── package.json           # 依赖配置
        ├── node_modules/          # 依赖包
        └── .env                   # 配置文件（首次使用时创建）
```


## 🎉 完成！

现在你可以：

1. **安装 Plugin**：复制到 `~/.claude/plugins/` 目录
2. **配置凭证**：首次使用时 Claude 会引导你
3. **开始使用**：直接告诉 Claude "把 xxx.md 发布到公众号"

## 💡 提示

- 封面图片是必需的
- 草稿不会自动发布，需要在公众平台手动发布
- 配置保存后可以重复使用
- 支持所有常见的 Markdown 语法

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看 SKILL.md 了解详细工作流程
2. 检查 .env 配置是否正确
3. 确认 IP 白名单已添加

祝使用愉快！🚀
